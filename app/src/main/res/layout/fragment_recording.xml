<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingTop="20dp">

    <!-- Top: Status Display -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/currentPositionCard"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="8dp"
        app:cardBackgroundColor="@color/md_theme_surfaceContainer"
        app:cardCornerRadius="16dp"
        app:cardElevation="4dp"
        app:strokeColor="@color/md_theme_outline"
        app:strokeWidth="1dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:paddingHorizontal="10dp"
            android:paddingVertical="6dp"
            android:gravity="center_vertical">

            <!-- Red Dot -->
            <ImageView
                android:id="@+id/redDot"
                android:layout_width="16dp"
                android:layout_height="16dp"
                android:contentDescription="@string/red_dot_description_text"
                android:src="@drawable/ic_baseline_red_dot_24"
                android:layout_marginEnd="4dp" />

            <!-- Distance Traveled -->
            <TextView
                android:id="@+id/currentDistanceTraveled"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/meter"
                android:textColor="@color/md_theme_secondary"
                android:textAppearance="@style/TextAppearance.Material3.LabelSmall"
                android:textStyle="bold" />

            <!-- Elevation -->
            <TextView
                android:id="@+id/currentElevation"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/elevation"
                android:textColor="@color/md_theme_secondary"
                android:textAppearance="@style/TextAppearance.Material3.LabelSmall"
                android:textStyle="bold"
                android:layout_marginStart="8dp"
                android:layout_marginEnd="8dp" />


            <!-- GNSS Error -->
            <TextView
                android:id="@+id/gnssError"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/gnss_error"
                android:textColor="@color/md_theme_error"
                android:textAppearance="@style/TextAppearance.Material3.LabelSmall"
                android:layout_marginStart="4dp" />

            <!-- WiFi Error -->
            <TextView
                android:id="@+id/wifiError"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/wifi_error"
                android:textColor="@color/md_theme_tertiary"
                android:textAppearance="@style/TextAppearance.Material3.LabelSmall"
                android:layout_marginStart="4dp" />

            <!-- Spacer: pushes signal icon to the far right -->
            <View
                android:layout_width="0dp"
                android:layout_height="0dp"
                android:layout_weight="1" />

            <!-- WiFi Signal Quality (3-bar indicator, pinned right) -->
            <com.openpositioning.PositionMe.presentation.view.WifiSignalView
                android:id="@+id/wifiSignalView"
                android:layout_width="20dp"
                android:layout_height="14dp" />
        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>

    <!-- Middle: Map Display -->
    <FrameLayout
        android:id="@+id/trajectoryMapFragmentContainer"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginStart="8dp"
        android:layout_marginEnd="8dp"
        app:layout_constraintTop_toBottomOf="@id/currentPositionCard"
        app:layout_constraintBottom_toTopOf="@id/controlLayout" />

    <!-- Compass Indicator (top-right corner of map) -->
    <com.openpositioning.PositionMe.presentation.view.CompassView
        android:id="@+id/compassView"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_marginTop="8dp"
        android:layout_marginEnd="12dp"
        android:alpha="0.85"
        android:elevation="6dp"
        app:layout_constraintTop_toBottomOf="@id/currentPositionCard"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Weather Indicator (left of compass) -->
    <com.openpositioning.PositionMe.presentation.view.WeatherView
        android:id="@+id/weatherView"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_marginEnd="6dp"
        android:alpha="0.85"
        android:elevation="6dp"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="@id/compassView"
        app:layout_constraintEnd_toStartOf="@id/compassView" />

    <!-- Test Point Button (bottom-left, red, mini) -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/btn_test_point"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="18dp"
        android:layout_marginBottom="32dp"
        android:contentDescription="Add test point"
        android:src="@android:drawable/ic_input_add"
        android:alpha="0.85"
        app:fabSize="mini"
        app:backgroundTint="#F44336"
        app:tint="@android:color/white"
        app:layout_constraintBottom_toTopOf="@id/controlLayout"
        app:layout_constraintStart_toStartOf="parent" />

    <!-- Upload Button (bottom-left, above test point, orange, mini) -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/btn_upload"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="18dp"
        android:layout_marginBottom="12dp"
        android:contentDescription="Upload trajectory"
        android:src="@android:drawable/ic_menu_upload"
        android:alpha="0.85"
        app:fabSize="mini"
        app:backgroundTint="#FF9800"
        app:tint="@android:color/white"
        app:layout_constraintBottom_toTopOf="@id/btn_test_point"
        app:layout_constraintStart_toStartOf="parent" />


    <!-- Bottom: Controls (Progress Bar and Buttons) -->
    <View
        android:id="@+id/calibrationTouchBlocker"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:visibility="gone"
        android:clickable="true"
        android:focusable="true"
        android:importantForAccessibility="no"
        android:background="#01000000"
        android:elevation="20dp"
        app:layout_constraintTop_toBottomOf="@id/currentPositionCard"
        app:layout_constraintBottom_toTopOf="@id/controlLayout"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <LinearLayout
        android:id="@+id/controlLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="12dp"
        android:gravity="center"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Progress Bar -->
        <com.google.android.material.progressindicator.LinearProgressIndicator
            android:id="@+id/timeRemainingBar"
            style="@style/Widget.Material3.LinearProgressIndicator"
            android:layout_width="match_parent"
            android:layout_height="20dp"
            android:max="1800"
            android:progress="0"
            android:layout_marginBottom="12dp" />

        <!-- Buttons Container -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center">

            <!-- Cancel Button (Tomato Red) -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/cancelButton"
                android:layout_width="0dp"
                android:layout_height="44dp"
                android:layout_weight="1"
                android:layout_marginEnd="6dp"
                android:alpha="1.0"
                android:text="@string/cancel"
                android:textSize="13sp"
                android:textAlignment="center"
                android:textColor="@android:color/white"
                app:backgroundTint="#FFFF6347"
                app:cornerRadius="12dp" />

            <!-- Print Location Button (Shark Grey) -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/printLocationButton"
                android:layout_width="0dp"
                android:layout_height="44dp"
                android:layout_weight="1"
                android:layout_marginEnd="6dp"
                android:alpha="1.0"
                android:text="@string/print_location"
                android:textSize="11sp"
                android:textAlignment="center"
                android:textColor="@android:color/white"
                app:backgroundTint="#FF838996"
                app:cornerRadius="12dp" />

            <!-- Complete Button (Mint Green) -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/stopButton"
                android:layout_width="0dp"
                android:layout_height="44dp"
                android:layout_weight="1"
                android:alpha="1.0"
                android:text="@string/stop"
                android:textSize="13sp"
                android:textAlignment="center"
                android:textColor="@android:color/white"
                app:backgroundTint="#FF3EB489"
                app:cornerRadius="12dp" />
        </LinearLayout>
    </LinearLayout>
</androidx.constraintlayout.widget.ConstraintLayout>
